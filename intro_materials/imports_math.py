# -*- coding: utf-8 -*-
# Math functions for complex numbers
from cmath import sqrt
from numpy.lib.scimath import np_sqrt
import numpy as np
from scipy import *

norm.cdf(np.array([-1., 0, 1]))

sqrt(-1)  # complex number with cmath

A = 1
B = 2
C = 100
R1 = (-B + sqrt(B**2 - 4 * A * C)) / (2 * A)
R2 = (-B - np_sqrt(B**2 - 4 * A * C)) / (2 * A)
